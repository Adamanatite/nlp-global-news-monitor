Sources:
URL (VARCHAR) PK
Name (VARCHAR)
Country (VARCHAR)
Language (VARCHAR)
Active (Binary)

Articles:
URL (VARCHAR) PK
Title (VARCHAR)
Text (VARCHAR)
Publish date (DATETIME)
Scrape date (DATETIME)
Source (FK to Sources)
---------------------------------------------------------------------------------------------------------------

Challenges:
    -How do we validate if a url works for a website
    -How to represent broken links (null text field?)
    -How do we determine language and country without explicitly stating it in input (through NLP?)
    -How do we determine source name without explicitly stating it in input?
    -What do we do with publish date when we can't scrape it (leave null?)
    -How to calculate stale fields? (get publish date of articles and check latest one? with a user defined threshold?)
    -Should we assume monolanguage sources? (Can we allow sources who publish in multiple languages, if so how do we represent this? Instead store language in article?)
    -More experimentation is required to decide the max length for e.g. article title